import Navigation from "./HeaderCopmponents/navigation/Navigation.tsx";


const Header = () => {
    return (
        <header>
            <Navigation/>
        </header>
    );
};

export default Header;